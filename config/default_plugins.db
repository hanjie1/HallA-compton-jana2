# Default JANA plugins to load
# Provide a comma-separated list of plugin names.
# Spaces before/after plugin names are ignored.
# Example:
# evio_parser,tracking,monitoring

evio_parser,evio_processor
